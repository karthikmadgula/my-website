services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: karthik-portfolio:latest
    container_name: karthik-portfolio
    ports:
      - "8080:80"
    restart: unless-stopped
